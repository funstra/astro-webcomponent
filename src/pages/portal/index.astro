---
import FilterList from "../../components/FilterList.astro";
import Layout from "../../layouts/Layout.astro";

const posts = (await Astro.glob("./*.astro")).map(({ url, title }) => ({
  title,
  url,
}));
---

<Layout title="Portal" pageOrder={1}>
  <FilterList list={posts} />
</Layout>
